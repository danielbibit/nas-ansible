
## BEGIN RCLONE ROLE ##
{% for entry in rclone_fstab %}
{{ entry.remote }}:{{ entry.remote_path }} {{ entry.mount_point }} rclone config=/etc/rclone.conf,uid={{ entry.uid | default(0) }},gid={{ entry.gid | default(0) }},umask={{ entry.umask | default('0022') }},{{ entry.options }} {{ entry.dump }} {{ entry.pass }}
{% endfor %}
## END RCLONE ROLE ##

# ultracc_webdav: /mnt/ultracc/webdav rclone config=/etc/rclone.conf,uid=1001,gid=100,umask=0022,defaults,_netdev,allow_other,vfs-cache-mode=writes,daemon-timeout=inf,buffer-size=128M,x-systemd.automount,x-systemd.idle-timeout=0 0 0
